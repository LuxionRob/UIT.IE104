{
  "name": "frontend",
  "version": "0.1.0",
  "private": true,
  "proxy": "http://localhost:3000",
  "dependencies": {
    "axios": "^1.1.3",
    "dotenv": "^16.0.3",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "0.9.5"
  },
  "scripts": {
    "start": "react-scripts start",
    "build-css": "tailwind build -i src/assets/tailwind.css -o src/tailwind.css --watch",
    "build": "react-scripts build && tailwind build -i src/assets/tailwind.css -o src/tailwind.css",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "devDependencies": {
    "tailwindcss": "^3.2.4"
  }
}
