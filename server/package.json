{
  "dependencies": {
    "@faker-js/faker": "^7.6.0",
    "json-server": "^0.17.1"
  },
  "scripts": {
    "seed": "node seeder.js",
    "seed-watch": "nodemon seeder.js",
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "npm i  && cd ../client && npm run build && mv build ../server/public && cd ../server"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
