{
  "dependencies": {
    "@faker-js/faker": "^7.6.0",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "joi": "^17.7.0",
    "json-server": "^0.17.1",
    "query-string": "^7.1.3"
  },
  "scripts": {
    "seed": "node seeder.js",
    "seed-watch": "nodemon seeder.js",
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "cd ../client && npm run build && mv build ../server/public && cd ../server"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
